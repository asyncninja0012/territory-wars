# Frontend Setup

## Steps Taken
1.  **Initialized Vite Project**: Created a new React + Vite project in `client/`.
    - Command: `npm create vite@latest client -- --template react`
    - Template: React (JavaScript)
2.  **Installed Dependencies**:
    - `npm install` (Automated by scaffold)
    - `socket.io-client`: For real-time communication with the backend.

## Next Steps
- Clear default boilerplate.
- Create `Grid` component.
- Integrate `socket.io-client` in `App.jsx`.
